---
import BaseHead from "../components/BaseHead.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import Hero from "../components/Hero.astro";
import ServiceCard from "../components/ServiceCard.astro";
import BookingButton from "../components/BookingButton.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import { getCollection } from "astro:content";

const siteSettings = await getCollection("siteSettings");
const siteData = siteSettings[0]?.data;

const services = await getCollection("services");
const sortedServices = services.sort((a, b) => a.data.order - b.data.order);
---

<!doctype html>
<html lang="nl">
  <head>
    <BaseHead
      title={`Behandelingen - ${siteData?.siteTitle || SITE_TITLE}`}
      description={`${siteData?.siteDescription || SITE_DESCRIPTION} - Professionele massagetherapie diensten inclusief Zweeds, deep tissue, hot stone en meer.`}
    />
  </head>
  <body>
    <Header />
    <main>
      <Hero
        title="Onze Massagebehandelingen"
        description="Ontdek ons assortiment professionele massagetherapieën ontworpen om uw specifieke wellnessbehoeften aan te pakken en u te helpen optimale gezondheid en ontspanning te bereiken."
      />

      <!-- Services Grid -->
      <section class="section section-light">
        <div class="container">
          <div class="grid grid-2">
            {
              sortedServices.map((service) => (
                <ServiceCard
                  title={service.data.title}
                  description={service.data.description}
                  duration={service.data.duration}
                  price={service.data.price}
                  featured={service.data.featured}
                />
              ))
            }
          </div>
        </div>
      </section>

      <!-- Pricing Information -->
      <section class="section">
        <div class="container">
          <div class="pricing-info">
            <h2>Investering in Uw Wellness</h2>
            <p>
              Onze prijzen weerspiegelen de kwaliteit van zorg, expertise en
              gepersonaliseerde aandacht die u bij elke sessie zult ontvangen.
            </p>

            <div class="pricing-grid">
              <div class="pricing-item">
                <h3>Pakketdeals</h3>
                <ul>
                  <li>
                    <strong>3 Sessies:</strong> Bespaar 10% op elke behandeling
                  </li>
                  <li>
                    <strong>5 Sessies:</strong> Bespaar 15% op elke behandeling
                  </li>
                  <li>
                    <strong>10 Sessies:</strong> Bespaar 20% op elke behandeling
                  </li>
                </ul>
              </div>
              <div class="pricing-item">
                <h3>Lidmaatschapsvoordelen</h3>
                <ul>
                  <li>Maandelijkse 60-minuten massage</li>
                  <li>10% korting op extra behandelingen</li>
                  <li>Priority boeking</li>
                  <li>Gratis aromatherapie</li>
                </ul>
              </div>
              <div class="pricing-item">
                <h3>Cadeaubonnen</h3>
                <p>
                  Geef het cadeau van ontspanning! Beschikbaar in elke waarde en
                  perfect voor verjaardagen, feestdagen of speciale
                  gelegenheden.
                </p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- What to Expect -->
      <section class="section section-light">
        <div class="container">
          <div class="what-to-expect">
            <h2>Wat U Kunt Verwachten</h2>
            <div class="expectations-grid">
              <div class="expectation-item">
                <h3>📋 Initieel Consult</h3>
                <p>
                  Uw sessie begint met een kort consult om uw
                  gezondheidsgeschiedenis, zorgen en wellnessdoelen te
                  bespreken.
                </p>
              </div>
              <div class="expectation-item">
                <h3>👕 Voorbereiding</h3>
                <p>
                  U heeft privacy om u uit te kleden tot uw comfortniveau en
                  comfortabel op de massagetafel te liggen, bedekt met lakens.
                </p>
              </div>
              <div class="expectation-item">
                <h3>👐 Gepersonaliseerde Behandeling</h3>
                <p>
                  Uw therapeut zal druk en technieken aanpassen op basis van uw
                  voorkeuren en behoeften gedurende de sessie.
                </p>
              </div>
              <div class="expectation-item">
                <h3>💧 Nazorg Begeleiding</h3>
                <p>
                  Ontvang aanbevelingen voor nazorg, inclusief hydratatie en
                  rekoefeningen om de voordelen te verlengen.
                </p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- CTA Section -->
      <section class="section">
        <div class="container">
          <div class="cta-section text-center">
            <h2>Klaar om Uw Wellnessreis te Beginnen?</h2>
            <p>
              Zet de eerste stap naar ontspanning en heling. Boek vandaag uw
              afspraak en ervaar de transformerende kracht van professionele
              massagetherapie.
            </p>
            <BookingButton
              text="Boek Nu Uw Sessie"
              size="large"
              variant="secondary"
            />
          </div>
        </div>
      </section>
    </main>
    <Footer />
  </body>
</html>

<style>
  .pricing-info {
    text-align: center;
    max-width: 1000px;
    margin: 0 auto;
  }

  .pricing-info h2 {
    margin-bottom: 1rem;
  }

  .pricing-info p {
    font-size: 1.1rem;
    color: var(--text-light);
    margin-bottom: 3rem;
    line-height: 1.6;
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    text-align: left;
  }

  .pricing-item {
    background: var(--white);
    padding: 2rem;
    border-radius: 10px;
    box-shadow: var(--shadow);
  }

  .pricing-item h3 {
    color: var(--accent);
    margin-bottom: 1rem;
  }

  .pricing-item ul {
    list-style: none;
    padding: 0;
  }

  .pricing-item li {
    padding: 0.5rem 0;
    border-bottom: 1px solid var(--gray-light);
    color: var(--text-light);
  }

  .pricing-item li:last-child {
    border-bottom: none;
  }

  .pricing-item p {
    color: var(--text-light);
    line-height: 1.6;
    margin: 0;
  }

  .what-to-expect {
    text-align: center;
    max-width: 1000px;
    margin: 0 auto;
  }

  .what-to-expect h2 {
    margin-bottom: 3rem;
  }

  .expectations-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
  }

  .expectation-item {
    text-align: center;
    padding: 2rem;
  }

  .expectation-item h3 {
    font-size: 1.3rem;
    margin-bottom: 1rem;
    color: var(--accent);
  }

  .expectation-item p {
    color: var(--text-light);
    line-height: 1.6;
  }

  .cta-section {
    max-width: 800px;
    margin: 0 auto;
    padding: 3rem 2rem;
    background: var(--secondary);
    border-radius: 15px;
    box-shadow: var(--shadow);
  }

  .cta-section h2 {
    margin-bottom: 1.5rem;
  }

  .cta-section p {
    margin-bottom: 2rem;
    font-size: 1.1rem;
    color: var(--text-light);
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .pricing-grid,
    .expectations-grid {
      grid-template-columns: 1fr;
    }

    .cta-section {
      padding: 2rem 1.5rem;
    }
  }
</style>
